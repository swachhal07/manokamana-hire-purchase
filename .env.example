# Frontend env. In dev you can leave this unset — Vite proxies /api to
# the local backend (localhost:5000). In production (Vercel) set it to your
# deployed backend, e.g. https://manokamana-api.onrender.com/api
VITE_API_URL=
